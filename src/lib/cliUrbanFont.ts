import { Rubik_Wet_Paint } from "next/font/google";

/** Graffiti / street-style display for CLI hero (Google Fonts). */
export const cliUrbanDisplay = Rubik_Wet_Paint({
  weight: "400",
  subsets: ["latin"],
  display: "swap",
});
